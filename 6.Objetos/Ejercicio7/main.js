const usuario={};

const job="developer";

usuario.firstName="Juan";
usuario.lastName="Castañeda";
usuario.age=25;
usuario.job=job;


console.log(`Mi nombre y mi apellido son  ${usuario.firstName} ${usuario.lastName}, tengo ${usuario.age} años y soy
${usuario.job}`);



usuario.firstName="keychron";
usuario.age=27;

console.log(`Mi nombre y mi apellido son  ${usuario.firstName} ${usuario.lastName}, tengo ${usuario.age} años y soy
${usuario.job}`);